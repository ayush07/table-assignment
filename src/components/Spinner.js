import React from 'react';

const Spinner = () => {
  return (
    <div
      style={{
        textAlign: 'center',
      }}
    >
      <i className='fa fa-spinner fa-spin' style={{ fontSize: '100px' }} />
    </div>
  );
};

export default Spinner;
